{
  "name": "bluefrog-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Bluefrog corporate landing page — static site",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
